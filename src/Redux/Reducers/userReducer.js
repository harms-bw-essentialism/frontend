const initialState = {
  user: {
    userId: null,
    token: null
  }
};

export const userReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
